{
  "name": "opus",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:sass": "sass src/styles.scss src/styles.css",
    "build:css": "postcss src/styles.css -o main.css",
    "build": "npm run build:sass && npm run build:css",
    "watch": "nodemon --watch src --ext scss --exec \"npm run build\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "postcss-cli": "^10.1.0",
    "tailwindcss": "^3.4.3",
    "nodemon": "^2.0.15",
    "sass": "^1.53.0"
  }
}
